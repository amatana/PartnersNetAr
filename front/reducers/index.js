import { ADD_PROJECT, ADD_USER } from "../actions/actionTypes";
import { createProject, createUser } from "../actions";

function reducer(action) {
  switch (action.type) {
    case ADD_PROJECT:
      break;

    case ADD_USER:
      break;
    default:
      break;
  }
}

export default reducer;
